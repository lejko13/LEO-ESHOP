import CatalogView from "../components/product/CatalogView.jsx";

// /produkty renders the same shared catalog UI as Home ("/") — see CatalogView.
const Shop = () => <CatalogView />;

export default Shop;
